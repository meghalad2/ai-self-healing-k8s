apiVersion: v1
kind: ConfigMap
metadata:
  name: sre-ai-agent-config
  namespace: production
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  DB_HOST: "postgres-service.production.svc.cluster.local"
  DB_PORT: "5432"
