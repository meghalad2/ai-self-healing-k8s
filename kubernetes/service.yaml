apiVersion: v1
kind: Service
metadata:
  name: sre-ai-agent
  namespace: production
  labels:
    app: sre-ai-agent
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: sre-ai-agent
